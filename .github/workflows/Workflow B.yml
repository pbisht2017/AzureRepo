name: Create Azure Storage Account

on:
  push:
    paths:
      - '.github/workflows/Workflow B.yml'

jobs:
  create-storage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Create Resource Group
        run: |
          az group create \
            --name myrs103085 \
            --location eastus

      - name: Create Azure Container Registry
        run: |
          az acr create \
            --resource-group myrs103085 \
            --name myprateekacr103085 \
            --sku Basic \
            --admin-enabled true

      
